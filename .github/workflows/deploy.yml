name: CI/CD Pipeline for Project-2

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get code for runner
        uses: actions/checkout@v4
      - name: Build Image
        run: docker build -t todo-list-image:v2 -f /c/Users/arman/OneDrive/Documents/DevOps-Projects/Project-2_AutomateTodoList/Dockerfile
  push:
    runs-on: ubuntu-latest
    steps:
      - name: Get code for runner
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Tag and push the image
        run: |
          docker tag todo-list-image:v2 moina512/python-todo-list-repo:v2
          docker push
          echo "All done!"
