name: CI/CD Pipeline for Project-2

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get code for runner
        uses: actions/checkout@v4
      - name: Build Image
        run: docker build . -t todo-list-image:v2 -f Project-2_AutomateTodoList/Dockerfile 
  push:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Get code for runner
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: moina512
          password: 190Lees!1106
      - name: Tag and push the image
        run: |
          docker image ls
          docker tag todo-list-image:v2 moina512/python-todo-list-repo:v2
          docker push moina512/python-todo-list-repo:v2
          echo "All done!"